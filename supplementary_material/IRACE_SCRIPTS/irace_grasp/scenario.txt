## Irace scenario configuration for GRASP hyperparameter tuning.
## Run from inside irace_grasp/ directory:
##   cd irace_grasp && Rscript -e "library(irace); irace.cmdline()"

# Training instances file (one absolute path per line)
trainInstancesFile = "./instances.txt"

# Target runner script (must be executable)
targetRunner = "./target-runner"

# Parameter space definition
parameterFile = "./parameters.txt"

# Total budget: number of target-runner calls
maxExperiments = 5000

# Reproducibility seed
seed = 12345

# Parallelism: 0 = auto-detect all available cores
parallel = 0

# Output log file
logFile = "./irace.Rdata"

# Irace verbosity (0=quiet, 1=normal, 2=verbose, 3=debug)
debugLevel = 1
